==== ubuntu ========

/etc/selinux/config

/etc/netplan/00-installer-config.yaml

sudo netplan apply

backupan
# This is the network config written by 'subiquity'
network:
  ethernets:
    enp0s3:
      dhcp4: false
      addresses:
        - 192.168.56.101/24
    enp0s8:
      dhcp4: true
  version: 2

yg bener
# This is the network config written by 'subiquity'
network:
  ethernets:
    enp0s3:
      dhcp4: true
    enp0s8:
      dhcp4: false
      addresses:
       - 192.168.56.2/24
  version: 2

network:
  version: 2
  renderer: networkd
  ethernets:
    ens33:                    # Nama interface jaringan (sesuaikan dengan interface di servermu, bisa menggunakan `ip a` untuk melihatnya)
      dhcp4: no                # Nonaktifkan DHCP
      addresses:
        - 192.168.1.100/24     # IP statis dan subnet mask
      gateway4: 192.168.1.1    # Alamat gateway
      nameservers:
        addresses:
          - 8.8.8.8            # Alamat DNS Google (atau sesuaikan)
          - 8.8.4.4