
===================================================== Upgrade MongoDB ======================================================

---------Berikut ini upgrade mongodb dari 4 ke 7 dengan bertahap --> 4 -> 5 -> 6 -> 7

============== pertama dari mongodb 4 ke mongodb 5 =======================================

1. PASTIKAN DATABASE BACKUP DAHULU

---- *PASTIKAN BACKUP DATABASE TERLEBIH DAHULU

- Backup MongoDB
mongodump --out /backup/mongodb/backup_$(date +%F_%H-%M-%S)

- Backup MongoDB memakai autentikasi (username/password)
mongodump --username root --password "welcome12345" --authenticationDatabase admin \
          --out /backup/mongodb/backup_$(date +%F_%H-%M-%S)

2. TAMBAHKAN REPO MONGODB 5.0 untuk Rocky Linux / RHEL8

sudo tee /etc/yum.repos.d/mongodb-org-5.0.repo <<EOF
[mongodb-org-5.0]
name=MongoDB Repository
baseurl=https://repo.mongodb.org/yum/redhat/8/mongodb-org/5.0/x86_64/
gpgcheck=1
enabled=1
gpgkey=https://www.mongodb.org/static/pgp/server-5.0.asc
EOF

3. INSTALL PAKET SERVER 5.0

sudo yum install -y mongodb-org-server-5.0.31 mongodb-org-mongos-5.0.31 mongodb-org-shell-5.0.31 mongodb-org-tools-5.0.31

4. MATIKAN SERVICE MONGODB VERSI 4

sudo systemctl stop mongod

5. Remove MongoDB 4.4 SERVER (AMAN, DATA TIDAK DIHAPUS)

sudo yum remove -y mongodb-org-server-4.4.29 mongodb-org-mongos-4.4.29 mongodb-org-shell-4.4.29 mongodb-org-tools-4.4.29

6. Hidupkan Service MongoDB 5.0

sudo systemctl start mongod

7. Cek version database mongodb

mongod --version

mongod --version
db version v5.0.31
Build Info: {
    "version": "5.0.31",
    "gitVersion": "973237567d45610d6976d5d489dfaaef6a52c2f9",
    "openSSLVersion": "OpenSSL 1.1.1k  25 Mar 2021",
    "modules": [],
    "allocator": "tcmalloc",
    "environment": {
        "distmod": "rhel80",
        "distarch": "x86_64",
        "target_arch": "x86_64"
    }
}

------- *MONGODB VERSI 5 BERHASIL DIINSTALL

- Setelah MongoDB 5.0 berjalan stabil
wajib set Feature Compatibility Version (FCV) 
--> untuk alasannya kenapa wajib di set ada di file FCV (Feature Compatibility Version)

(masuk sebagai root)

mongo --username root --authenticationDatabase admin
MongoDB shell version v5.0.31
Enter password:
connecting to: mongodb://127.0.0.1:27017/?authSource=admin&compressors=disabled&gssapiServiceName=mongodb
Implicit session: session { "id" : UUID("76c95c88-bac0-40a1-b30a-88dc6299fab5") }
MongoDB server version: 5.0.31

> use admin
switched to db admin
> db.adminCommand({ setFeatureCompatibilityVersion: "5.0" })
{ "ok" : 1 }
>

*JIKA SUKSES MAKA AKAN MUNCULKAN OK 1


====================== SELANJUTNYA UPGRADE DARI MONGODB VERSI 5 ke 6 ============================

1 BACKUP

---- *PASTIKAN BACKUP DATABASE TERLEBIH DAHULU

opsi 1

- Backup MongoDB
mongodump --out /backup/mongodb/backup_$(date +%F_%H-%M-%S)

- Backup MongoDB memakai autentikasi (username/password)
mongodump --username root --password "welcome12345" --authenticationDatabase admin \
          --out /backup/mongodb/backup_$(date +%F_%H-%M-%S)

opsi 2

- Matikan service database
systemctl stop mongod
systemctl status mongod

- Backup full data direktori
cp -a /var/lib/mongo /var/lib/mongo-backup-before-6

- Hidupkan kembali service database
systemctl status mongod
systemctl start mongod

2. Disable & remove paket MongoDB 5.0

- Matikan service database
systemctl stop mongod

- uninstall mongodb versi 5
sudo yum erase -y mongodb-org mongodb-org-server mongodb-org-shell mongodb-org-mongos mongodb-org-tools

3. Tambahkan MongoDB 6.0 Repository

- buat repo baru mongodb versi 6

sudo tee /etc/yum.repos.d/mongodb-org-6.0.repo <<EOF
[mongodb-org-6.0]
name=MongoDB Repository
baseurl=https://repo.mongodb.org/yum/redhat/8/mongodb-org/6.0/x86_64/
gpgcheck=1
enabled=1
gpgkey=https://www.mongodb.org/static/pgp/server-6.0.asc
EOF

4. Install MongoDB 6.0

sudo yum install -y mongodb-org

5. Start MongoDB 6.0

systemctl status mongod
systemctl start mongod
systemctl enable mongod

6. Cek version database mongodb

mongod --version

output = 
db version v6.0.26
Build Info: {
    "version": "6.0.26",
    "gitVersion": "0c4ec4b6005f75582ce208fc800f09f561b6c2e8",
    "openSSLVersion": "OpenSSL 1.1.1k  25 Mar 2021",
    "modules": [],
    "allocator": "tcmalloc",
    "environment": {
        "distmod": "rhel80",
        "distarch": "x86_64",
        "target_arch": "x86_64"
    }
}


7. Cek status database

mongosh --username root --authenticationDatabase admin
password : welcome12345

db.runCommand({ serverStatus: 1 })

***PASTIKAN TIDAK ADA error

8. Set Feature Compatibility Version (FCV) ke 6.0

*** WAJIB (kalau tidak, upgrade ke 7.0 nanti gagal)

mongosh --username root --authenticationDatabase admin
password : welcome12345

use admin
db.adminCommand({ setFeatureCompatibilityVersion: "6.0" })

***PASTIKAN OUTPUT HARUS OK : 1



====================== SELANJUTNYA UPGRADE DARI MONGODB VERSI 5 ke 6 ============================

1. CEK VERSI DATABASE MONGODB
mongod --version

Pastikan output version 6.0.x.

2. Set Feature Compatibility Version (FCV) ke 6.0
mongosh --username root --authenticationDatabase admin
password : welcome12345

db.adminCommand({ getParameter: 1, featureCompatibilityVersion: 1 }) --> jalankan ini maka akan
{ featureCompatibilityVersion: { version: '6.0' }, ok: 1 } ----> munculkan output ini

3. MATIKAN SERVICE
systemctl stop mongod
systemctl status mongod

4. REMOVE PAKET MONGODB 6 (AMAN, TIDAK MENGHAPUS DATA)
yum remove -y mongodb-org-server-6.0.26 \
              mongodb-org-mongos-6.0.26 \
              mongodb-org-shell-6.0.26 \
              mongodb-org-tools-6.0.26 \
              mongodb-org-database-6.0.26 \
              mongodb-org-database-tools-extra-6.0.26


5. TAMBAH REPOSITORY MONGODB 7.0
nano /etc/yum.repos.d/mongodb-org-7.0.repo

[mongodb-org-7.0]
name=MongoDB 7.0 Repository
baseurl=https://repo.mongodb.org/yum/redhat/$releasever/mongodb-org/7.0/x86_64/
gpgcheck=1
enabled=1
gpgkey=https://www.mongodb.org/static/pgp/server-7.0.asc

6. UPGRADE KE MONGODB 7.0
yum install -y mongodb-org

7. CEK VERSI BINARY

mongod --version
db version v7.0.26
Build Info: {
    "version": "7.0.26",
    "gitVersion": "715b3bbc6c34cecc42ffc77ef77d24f71e240e94",
    "openSSLVersion": "OpenSSL 3.2.2 4 Jun 2024",
    "modules": [],
    "allocator": "tcmalloc",
    "environment": {
        "distmod": "rhel90",
        "distarch": "x86_64",
        "target_arch": "x86_64"
    }
}


8. HIDUPKAN SERVICE
systemctl start mongod
systemctl status mongod


9. SET FCV 
mongosh --username root --authenticationDatabase admin

admin> db.adminCommand({ setFeatureCompatibilityVersion: "7.0", confirm: true }

admin> db.adminCommand({ getParameter: 1, featureCompatibilityVersion: 1 })


10. MASUK MONGOSH & CEK VERSI
 mongosh --username root --authenticationDatabase admin
Enter password: ************
test> db.version()
7.0.26
